
defineClass("ViewController", {
            clickAction: function(button) {
            require('UIButton, UIColor,UIView');
            var blue = UIColor.blueColor();
            button.setBackgroundColor(blue);
            self.view.setBackgroundColor(UIColor.redColor);
            }
            
            })
